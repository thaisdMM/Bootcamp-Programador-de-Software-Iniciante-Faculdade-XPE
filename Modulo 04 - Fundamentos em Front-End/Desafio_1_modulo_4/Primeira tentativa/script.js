var ncels = document.querySelectorAll("td");

function iniciatd(jv) {
  var addButton = document.createElement("button");
  addButton.click = "escolherJogada";
  addButton.type = "button";
}

jv.appendChild(addButton);
